import * as chai from 'chai';
import * as sinonModule from 'sinon';
import mongoose from 'mongoose';

const expect = chai.expect;
const sinon = sinonModule.default;

export { expect, sinon, mongoose };